[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gotg"
version = "0.1.0"
description = "AI agents collaborating like real engineering teams"
readme = "README.md"
license = "MIT"
authors = [{name = "biff-ai"}]
requires-python = ">=3.11"
dependencies = ["httpx"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development",
]

[project.urls]
Homepage = "https://github.com/MBifolco/gotg"
Repository = "https://github.com/MBifolco/gotg"
"Bug Tracker" = "https://github.com/MBifolco/gotg/issues"

[project.optional-dependencies]
tui = [
    "textual>=7.5,<7.6",
]

[project.scripts]
gotg = "gotg.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
gotg = ["data/*.toml", "tui/*.tcss"]

[tool.pytest.ini_options]
testpaths = ["tests"]
